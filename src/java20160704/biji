重写：子类方法签名与父类一样，子类覆盖了父类的方法
     子类的重写父类的方法名相同 参数不用 放回值类型不同 访问修饰符>=父类的访问修饰符，跑出的异常<=父类
重载：在同一个类中。方法名相同，参数列表不同，与返回值无关，异常无关，访问修饰符无关
子类不可以重写父类的构造方法
每个构造方法中都会默认加上一个super()；用于调用父类的构造方法。super();必须写在第一行
当子类的方法名与父类的属性名相同的时候我们用super()。属性名访问父类的属性。
abstract修饰的类不能别实例化，abstract修饰的方法是不能有代码实现的，必须重写。
抽象的方法必须存在抽象的类中，但是抽象类中不一定有抽象方法。
子类继承抽象类，子类必须实现父类中的所有的抽象的方法，如果没有实现必须浪自己变成抽象类，让自己的的子类来实现。
